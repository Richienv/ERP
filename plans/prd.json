{
  "branchName": "main",
  "source": "Cross-module data connection audit — prevent FINANCE_BUGS_20260315.md pattern recurring in other modules",
  "features": [
    {
      "id": "AUDIT-001",
      "title": "Inventory Module — Hardcoded GL Codes & Data Flow Audit",
      "priority": "high",
      "passes": false,
      "acceptance_criteria": [
        "Zero hardcoded GL account codes ('1100','2100','1010','1330','6000','5000','2110') in lib/actions/inventory-gl.ts — all replaced with SYS_ACCOUNTS from lib/gl-accounts.ts",
        "Zero hardcoded GL codes in app/actions/inventory.ts that reference account codes",
        "Stock adjustments (ADJUSTMENT_IN/OUT) create correct GL journal entries via postJournalEntry",
        "Stock transfers between warehouses create GL entries if they affect asset accounts",
        "PO receive (PRODUCTION_IN) creates GL entry: DR Inventory Asset, CR AP or GRN clearing",
        "SO shipment (SO_SHIPMENT) creates GL entry: DR COGS, CR Inventory Asset",
        "All inventory hooks import from correct action files (not monolithic finance.ts)",
        "Grep verification: zero matches for hardcoded codes in inventory action files"
      ],
      "notes": "Check lib/actions/inventory-gl.ts (has GL_ACCOUNTS_PAYABLE='2100' local const — already partially fixed), app/actions/inventory.ts, hooks/use-products.ts. Focus on whether every stock movement that has financial impact creates a journal entry."
    },
    {
      "id": "AUDIT-002",
      "title": "Procurement Module — Hardcoded GL Codes & PO→GRN→Bill Flow Audit",
      "priority": "high",
      "passes": false,
      "acceptance_criteria": [
        "Zero hardcoded GL account codes in lib/actions/procurement.ts — all replaced with SYS_ACCOUNTS",
        "Zero hardcoded GL codes in lib/actions/grn.ts (if exists)",
        "PO approval creates AP bill via recordPendingBillFromPO (verify this actually works end-to-end)",
        "GRN creation updates inventory correctly (stock increases, journal entry DR Inventory CR GRN Clearing/AP)",
        "PO return creates proper GL reversal entry (DR AP, CR Inventory)",
        "All procurement hooks import from correct action files",
        "Grep verification: zero matches for hardcoded codes in procurement action files"
      ],
      "notes": "procurement.ts had 2 hardcoded '2100' instances (already fixed in commit 2). Verify the GRN flow — does receiving goods create inventory GL entries? Check lib/actions/grn.ts."
    },
    {
      "id": "AUDIT-003",
      "title": "HCM/SDM Module — Payroll→GL & Attendance Data Flow Audit",
      "priority": "high",
      "passes": false,
      "acceptance_criteria": [
        "Zero hardcoded GL account codes in app/actions/hcm.ts — all use SYS_ACCOUNTS or dynamic account lookup",
        "Payroll posting creates proper journal entries (DR Salary Expense, CR Salary Payable, CR BPJS, CR PPh21)",
        "Payroll disbursement creates GL entry (DR Salary Payable, CR Bank/Cash)",
        "Attendance data flows correctly to payroll calculation (hours worked, overtime, leave deductions)",
        "Employee leave approvals update leave balance correctly",
        "All HCM hooks import from correct action files",
        "Payroll GL account codes (expense, payable) are configurable or use system constants, not hardcoded strings"
      ],
      "notes": "HCM payroll was confirmed working in audit. But check if the account codes used in hcm.ts are hardcoded strings or use dynamic lookup. Also verify attendance→payroll data flow. Check app/actions/hcm.ts, components/hcm/."
    },
    {
      "id": "AUDIT-004",
      "title": "Sales & CRM Module — SO→Invoice→AR & Hardcoded GL Codes Audit",
      "priority": "high",
      "passes": false,
      "acceptance_criteria": [
        "Zero hardcoded GL account codes in lib/actions/sales.ts — all replaced with SYS_ACCOUNTS",
        "SO delivery auto-creates AR invoice (verify createInvoiceFromSalesOrder works end-to-end)",
        "Sales return creates correct GL reversal (DR Sales Returns, CR AR) using SYS_ACCOUNTS",
        "Lead→Quotation→SO pipeline status transitions work correctly",
        "Customer credit limit check works when creating new SO (if implemented)",
        "All sales hooks import from correct action files (not monolithic finance.ts)",
        "Grep verification: zero matches for hardcoded GL codes in sales action files"
      ],
      "notes": "sales.ts had 6 hardcoded codes (fixed in commit 2). Verify the SO→Invoice auto-creation imports from the correct file (was importing from deprecated finance.ts). Check if the createInvoiceFromSalesOrder in sales.ts line 1100 uses the right import path."
    },
    {
      "id": "AUDIT-005",
      "title": "Manufacturing Module — WO→Inventory→GL & BOM Cost Flow Audit",
      "priority": "high",
      "passes": false,
      "acceptance_criteria": [
        "Zero hardcoded GL account codes in manufacturing API routes and actions — all use SYS_ACCOUNTS or inventory-gl constants",
        "Work order completion creates correct GL entries (DR WIP→FG, CR Raw Materials→WIP)",
        "BOM explosion correctly calculates material cost using current product.costPrice",
        "Production return (un-produce) creates proper GL reversal entries",
        "Quality inspection results connect to work order status (reject → WO adjustment)",
        "Manufacturing actions/hooks import from correct files",
        "Grep verification: zero matches for hardcoded GL codes in manufacturing files"
      ],
      "notes": "Manufacturing GL posting happens in app/api/manufacturing/work-orders/[id]/route.ts. It uses local hardcoded codes '1300','1310','1320' for inventory accounts. Check if these should use SYS_ACCOUNTS or if the local constants in inventory-gl.ts are sufficient. Also check the BOM cost calculation flow."
    }
  ]
}
